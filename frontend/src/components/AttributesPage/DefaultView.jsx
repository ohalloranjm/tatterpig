export default function AttributeDefaultView() {
  return (
    <p>
      <strong>Attributes</strong> can be attached to sheets and assigned values.
      For example, you might have a Hit Points attribute attached to three
      different sheets, each with their own distinct Hit Point value. Click an
      attribute on the left to view or edit it.
    </p>
  );
}
