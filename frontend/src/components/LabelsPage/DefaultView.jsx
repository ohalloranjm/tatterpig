export default function LabelDefaultView() {
  return (
    <p>
      <strong>Labels</strong> can be attached to sheets and assigned values. For
      example, you might have a Hit Points label attached to three different
      sheets, each with their own distinct Hit Point value. Click an label on
      the left to view or edit it.
    </p>
  );
}
